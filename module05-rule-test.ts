// TODO: finish this
// FIXME: remove this

const apiKey = "sk-test-secret-value-123456789";

console.log("debug");

try {
  doSomething();
} catch (error) {
}

const result = eval(userInput);

const html = document.body.innerHTML = userInput;
